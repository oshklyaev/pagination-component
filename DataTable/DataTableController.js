({
    getData : function(component, event, helper) {
        helper.getTempData(component, event, helper);
    },

    switchFilter : function(component, event, helper) {
        helper.switchDataFilter(component, event, helper);
    }
})